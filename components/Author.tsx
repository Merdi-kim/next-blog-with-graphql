import React from 'react'
import { IAuthorProps } from '../types'

function Author({author}:IAuthorProps) {
  return (
    <div>{/** This will become the bottom part of every article */}</div>
  )
}

export default Author